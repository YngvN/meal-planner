/** Nutritional breakdown, typically expressed per 100 g or per serving. */
export interface NutritionalValues {
  calories?: number
  protein?: number
  carbs?: number
  fat?: number
  fiber?: number
}
